name: Deploy

# Activé en P5 quand la cible Axya est confirmée avec Axel.
# Laisser disabled jusqu'à décision de déploiement (voir PROJECT_V1.md §10).

on:
  workflow_dispatch:
  # push:
  #   branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build Docker image
        run: docker build -t outil-valo .
      # TODO P5 : push registry + déploiement Axya
